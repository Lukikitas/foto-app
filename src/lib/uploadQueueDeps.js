import { compressImage, EVIDENCE_IMAGE_OPTIONS } from './compressImage.js';
import { uploadFile, uploadPhoto, uploadUnidentifiedOrder } from './photos.js';

export {
  compressImage,
  EVIDENCE_IMAGE_OPTIONS,
  uploadFile,
  uploadPhoto,
  uploadUnidentifiedOrder,
};
